# hadlink Docker configuration
# Edit values directly or use deploy.sh with command-line options

# Service ports
REDIRECT_PORT=8080
SHORTEN_PORT=8443

# Proof-of-work difficulty (0 = disabled)
# Each +1 doubles the average computation required
POW_DIFFICULTY=0
POW_DIFFICULTY_AUTH=0

# Rate limit per IP per minute
RATE_LIMIT=10
